## Project Constraints

- Static HTML-only project (GitHub Pages)
- No backend, no Node.js, no npm
- No test frameworks (Playwright, Jest, etc.)
- All parsing logic must run client-side (vanilla JS)

AI agents must respect these constraints.
